spring:
  application:
    name: APIGateway
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true
          routes:
            - id: review-service
              uri: lb://review-service
              predicates:
                - Path=${prefix}/reviews/**
              filters:
                - RewritePath=${prefix}/reviews/(?<segment>.*), /${segment}

            # Swagger Docs of Book Service
            - id: review-service-docs
              uri: lb://review-service
              predicates:
                - Path=${prefix}/v3/api-docs
              filters:
                - RewritePath=${prefix}/(?<segment>.*), /${segment}
    compatibility-verifier:
      enabled: true
server:
  port: 8080
springdoc:
  api-docs:
      enabled: true
      path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    urls:
      - name: review-service
        url: /v3/api-docs
prefix: /v1/api
